/*
 * Hingenia Insignias — admin JS (placeholder de Fase 1).
 *
 * Aquí irán en fases siguientes:
 *  - Editor visual de plantillas (drag/resize de nombre y QR sobre canvas).
 *  - Modal de subida con preview.
 *  - Importador CSV con preview + barra de progreso.
 *  - Búsqueda en vivo de la tabla de emisiones.
 */
( function ( $ ) {
	'use strict';
	$( function () {
		// nada por ahora.
	} );
} )( jQuery );
